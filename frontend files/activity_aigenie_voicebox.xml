<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ocean_shore"
    android:padding="16dp">

    <!-- AI-Genie Voicebox Header -->
    <LinearLayout
        android:id="@+id/header_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:paddingVertical="16dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true">

        <!-- Small logo box -->
        <ImageView
            android:id="@+id/logo_icon"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:src="@drawable/genie_purple_image"
            android:contentDescription="Logo"
            android:layout_marginEnd="8dp" />

        <!-- Text inside the box -->
        <TextView
            android:id="@+id/header_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI-Genie voicebox"
            android:textStyle="bold"
            android:textColor="#000000"
            android:textSize="29sp" />

    </LinearLayout>


    <!-- Listening Status Text -->
    <TextView
        android:id="@+id/listening_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Speak now – I'm listening to your preferences"
        android:textColor="#000000"
        android:textSize="25sp"
        android:gravity="center"
        android:padding="16dp"
        android:background="@drawable/oval_silver_box"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/header"
        android:layout_marginTop="60dp" />



    <!-- Microphone Icon -->
    <!-- This should be your mic icon inside a purple circle -->
    <ImageView
        android:id="@+id/mic_icon"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:background="@drawable/circle_purple_bg"
        android:layout_centerInParent="true"
        android:padding="16dp"
        android:src="@drawable/speaker_option"
        android:scaleType="centerInside" />


</RelativeLayout>
